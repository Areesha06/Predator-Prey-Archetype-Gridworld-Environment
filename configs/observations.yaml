observations:

  # type: local_only
  # params:
  #   include_agents: true
  #   include_obstacles: true
  #   radius: 3
  per_agent:
    predator:
      radius: 4
    prey:
      radius: 2

  type: relative
  params:
    include_agents: true
    include_obstacles: false
    include_walls: false
    distance_type: manhattan

  # type: local_only
  # params: {}
